.topbar {
  background-color: rgba(215, 215, 215, 1);
  height: 30px;
  line-height: 30px;
  font-size: 13px;

  .user {
    margin: 0;
    padding: 0;
    display: flex;
    justify-content: flex-end;
    list-style: none;

    li {
      padding: 0 20px;
    }

    .eng-or-ch {
        font-size :0;
        button {
            font-size:12px;
            padding 8px 0px;
            margin-left :5px;
        }
    }
  }
}